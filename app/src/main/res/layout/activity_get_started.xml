<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- ImageView for the background image -->
    <ImageView
        android:id="@+id/background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/intro2" /> <!-- Removed background -->

    <!-- Gradient overlay -->
    <View
        android:id="@+id/overlay"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:background="@drawable/gradient_overlay"
        android:layout_alignParentBottom="true" />


    <!-- Button to get started -->
    <Button
        android:id="@+id/get_started_button"
        android:layout_width="340dp"
        android:layout_height="wrap_content"
        android:text="Get Started"
        android:textColor="@android:color/black"
        android:layout_marginTop="2dp"
        android:textSize="20sp"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="50dp"
        android:padding="10dp"
        android:elevation="18dp"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/darklilac"
        />
    <!-- TextView for the description -->
    <TextView
        android:id="@+id/description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Fitness is a Journey \nNot a Destination"
        android:gravity="center"
        android:padding="10dp"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="25sp"
        android:layout_above="@id/get_started_button"
        android:layout_marginBottom="15dp"/> <!-- Aligned below overlay -->

</RelativeLayout>